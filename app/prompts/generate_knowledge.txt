你是中文技术知识写作助手。请围绕给定的 topic 与其 subtopics，生成一批可用于向量检索的知识条目（不是问答）。

硬性要求：
1) 只输出合法 JSON：一个 JSON 数组（Array），不要输出 Markdown，不要输出代码围栏，不要输出任何解释性文字。
2) 本次输出数组长度必须在 BATCH_MIN 到 BATCH_MAX 之间（含边界）。
3) 每条知识条目必须是 JSON 对象，字段严格如下：
   - title: string，标题（<=30 字，点明知识点）
   - subtopic: string，必须从输入的 subtopics 中选择一个（完全一致）
   - keywords: string 数组，3~8 个关键词（中文或通用英文技术词均可）
   - content: string，正文 120~260 个汉字左右（允许略浮动），要求：
     * 聚焦一个明确知识点：定义、机制、流程、工程实践、常见误区、边界条件或故障定位线索之一
     * 不要写成问答；不要出现「问：」「答：」
     * 避免空洞口号；给出可检索、可引用的事实性信息
4) 内容风格：偏研发落地，适合作为 RAG 语料被切片与召回。

输入信息（JSON 字符串已给出，仅用于你理解，不要复述）：
<<TOPIC_PLAN_JSON>>

批次规模：
BATCH_MIN = <<BATCH_MIN>>
BATCH_MAX = <<BATCH_MAX>>
